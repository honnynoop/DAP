# DAP 자격시험 주관식 서술문제 10선

> 각 문제는 실제 DAP 시험 출제 경향을 반영한 서술형 문제입니다.  
> 답안 작성 후 해설을 확인하여 부족한 부분을 보완하세요.

---

## 문제 1. 정규화와 역정규화의 Trade-off

**[문제]**  
데이터베이스 설계 시 정규화(Normalization)와 역정규화(De-normalization)의 개념을 설명하고, 실무에서 역정규화를 적용하는 주요 사례와 그에 따른 부작용 및 관리 방안을 서술하시오.

---

### 해설

#### 1) 정규화(Normalization)
정규화란 데이터의 **중복을 최소화**하고 **갱신 이상(Update Anomaly)** 을 제거하기 위해 릴레이션을 분해하는 과정이다.

| 정규형 | 조건 |
|--------|------|
| 1NF | 원자값(Atomic Value), 반복 그룹 제거 |
| 2NF | 1NF + 부분 함수 종속 제거 |
| 3NF | 2NF + 이행 함수 종속 제거 |
| BCNF | 3NF + 모든 결정자가 후보키 |
| 4NF | BCNF + 다치 종속 제거 |
| 5NF | 4NF + 조인 종속 제거 |

**갱신 이상의 종류:**
- **삽입 이상**: 불필요한 데이터를 함께 삽입해야 하는 현상
- **삭제 이상**: 관련 없는 데이터까지 함께 삭제되는 현상
- **수정 이상**: 중복된 데이터 중 일부만 수정되어 불일치가 발생하는 현상

#### 2) 역정규화(De-normalization)
역정규화란 **성능 향상**을 목적으로 의도적으로 정규화된 데이터를 합치거나 중복을 허용하는 설계 기법이다.

**역정규화 적용 사례:**

| 유형 | 설명 | 예시 |
|------|------|------|
| 테이블 합병 | 자주 조인되는 테이블을 하나로 합침 | 주문 + 주문상세 합병 |
| 컬럼 중복 | 자주 조회되는 값을 다른 테이블에 복사 | 주문 테이블에 고객명 컬럼 추가 |
| 파생 컬럼 추가 | 계산값을 미리 저장 | 주문 테이블에 합계금액 컬럼 추가 |
| 테이블 분리 | 대용량 테이블을 수평/수직 분리 | 이력 테이블 분리 |

**역정규화의 부작용:**
- 데이터 중복으로 인한 **일관성 훼손** 위험
- **DML 성능 저하** (INSERT/UPDATE/DELETE 시 여러 곳 반영 필요)
- 저장 공간 증가

**관리 방안:**
- 중복 컬럼은 **트리거(Trigger)** 또는 **애플리케이션 로직**으로 동기화
- 도입 전 성능 측정(SQL 실행계획 분석) 후 적용
- 설계 문서에 역정규화 사유 명시 및 이력 관리

---

## 문제 2. 함수 종속성과 BCNF

**[문제]**  
다음 릴레이션 R(학생번호, 과목코드, 교수명, 성적)에서 함수 종속성이 아래와 같을 때, 이 릴레이션이 BCNF를 위반하는 이유를 설명하고, BCNF를 만족하도록 분해하시오.

- FD1: {학생번호, 과목코드} → 성적
- FD2: {교수명} → 과목코드
- FD3: {학생번호, 과목코드} → 교수명
- (하나의 과목은 여러 교수가 담당할 수 있으며, 한 학생은 동일 과목을 여러 교수에게 수강할 수 없다고 가정)

---

### 해설

#### 1) 후보키 식별
주어진 FD를 분석하면:
- {학생번호, 과목코드} → 성적, 교수명 (모든 속성 결정) → **후보키①**
- {학생번호, 교수명} → 과목코드(FD2 역) → 성적 → **후보키②**

따라서 후보키: `{학생번호, 과목코드}`, `{학생번호, 교수명}`

#### 2) BCNF 위반 확인
**BCNF 조건**: 릴레이션의 **모든 결정자(Determinant)가 후보키**여야 한다.

FD2: `교수명 → 과목코드`에서 결정자 `교수명`은 단독으로 후보키가 **아니다**.  
→ **BCNF 위반**

#### 3) BCNF 분해

**분해 원칙**: 위반하는 FD(`교수명 → 과목코드`)를 기준으로 분리

| 분해 후 릴레이션 | 속성 | 후보키 |
|----------------|------|--------|
| R1 | (교수명, 과목코드) | {교수명} |
| R2 | (학생번호, 교수명, 성적) | {학생번호, 교수명} |

- **R1(교수명, 과목코드)**: 교수명 → 과목코드 (교수명이 후보키 → BCNF 만족)
- **R2(학생번호, 교수명, 성적)**: {학생번호, 교수명} → 성적 (후보키가 결정자 → BCNF 만족)

> **주의**: BCNF 분해는 **무손실 조인(Lossless Join)** 은 보장하지만,  
> **함수 종속성 보존(Dependency Preservation)** 은 항상 보장하지 않는다.  
> 이 경우 FD1({학생번호, 과목코드} → 성적)은 단일 릴레이션에서 직접 표현되지 않으므로 의존성 보존이 깨짐에 유의.

---

## 문제 3. 데이터 표준화

**[문제]**  
엔터프라이즈 데이터 관리에서 데이터 표준화의 구성 요소(표준 단어, 표준 도메인, 표준 코드, 표준 용어)를 각각 정의하고, 표준화 절차와 표준화가 미치는 효과를 설명하시오.

---

### 해설

#### 1) 데이터 표준화의 4대 구성 요소

| 구성 요소 | 정의 | 예시 |
|----------|------|------|
| **표준 단어** | 업무에서 사용하는 단어의 표준 명칭 및 약어 정의 | 고객(CUST), 주문(ORD) |
| **표준 도메인** | 데이터 속성의 논리적 유형과 물리적 데이터 타입 정의 | 금액도메인: NUMBER(15,2) |
| **표준 코드** | 데이터 값의 목록과 의미를 표준화 | 성별코드: M=남성, F=여성 |
| **표준 용어** | 표준 단어를 조합하여 만든 엔터티/속성의 표준 명칭 | 고객번호(CUST_NO) |

#### 2) 데이터 표준화 절차

```
① 현행 데이터 현황 분석 (As-Is 파악)
     ↓
② 표준화 원칙 및 정책 수립
     ↓
③ 표준 단어 정의 (단어 사전 작성)
     ↓
④ 표준 도메인 정의 (도메인 사전 작성)
     ↓
⑤ 표준 코드 정의 (코드 사전 작성)
     ↓
⑥ 표준 용어 정의 (용어 사전 작성)
     ↓
⑦ 표준화 준수 여부 검증 및 배포
     ↓
⑧ 지속적 관리 및 유지보수
```

#### 3) 데이터 표준화의 효과

- **상호 운용성 향상**: 시스템 간 데이터 연계 시 매핑 오류 감소
- **데이터 품질 향상**: 동일 개념을 동일한 방식으로 표현하여 일관성 확보
- **유지보수 비용 절감**: 개발자/운영자 간 의사소통 오류 감소
- **데이터 통합 용이**: EAI/ESB, 데이터 웨어하우스 구축 시 변환 비용 최소화
- **규정 준수**: 금융감독원, 공공기관 데이터 표준 지침 준수 가능

---

## 문제 4. 데이터 모델링 3단계

**[문제]**  
데이터 모델링의 3단계(개념적, 논리적, 물리적)를 각 단계별 목적, 주요 산출물, 수행 주체, 사용 표기법의 관점에서 비교 설명하시오.

---

### 해설

#### 데이터 모델링 3단계 비교

| 구분 | 개념적 모델링 | 논리적 모델링 | 물리적 모델링 |
|------|-------------|-------------|-------------|
| **목적** | 업무 중심의 핵심 데이터 구조 파악 | 데이터 간 관계와 규칙을 상세하게 표현 | DBMS에 최적화된 구현 구조 설계 |
| **주요 산출물** | 개념 ERD (핵심 엔터티, 관계) | 논리 ERD (속성, 식별자, 정규화 완료) | 물리 ERD (테이블, 컬럼, 인덱스, 파티션) |
| **수행 주체** | 업무 전문가 + DA | DA + 설계자 | DBA + 개발자 |
| **표기법** | Peter Chen 표기법, IE 표기법 (단순) | IE 표기법, Barker 표기법 | DBMS 종속적 DDL |
| **DBMS 독립성** | 완전 독립 | 독립 | 종속 |
| **정규화** | 불필요 | 완전 정규화 수행 | 역정규화 고려 |

#### 각 단계 상세 설명

**① 개념적 모델링**
- 비즈니스 관점에서 핵심 엔터티와 관계만 도출
- 속성은 핵심 식별자 수준만 표현
- 목표: 업무 이해관계자와의 의사소통 도구

**② 논리적 모델링**
- 모든 속성, 식별자, 관계, 제약조건을 명확히 표현
- 정규화를 통해 데이터 무결성 확보
- 목표: 구현을 위한 완전한 데이터 설계도

**③ 물리적 모델링**
- 특정 DBMS 환경에 맞게 변환 (데이터 타입, 컬럼명 등)
- 성능을 위한 인덱스 설계, 파티셔닝, 역정규화 적용
- 목표: 최적 성능의 실제 데이터베이스 구현

---

## 문제 5. 식별자 관계 vs 비식별자 관계

**[문제]**  
엔터티 간 관계를 설계할 때 식별자 관계(Identifying Relationship)와 비식별자 관계(Non-Identifying Relationship)의 개념적 차이를 설명하고, 각각의 장단점과 실무 적용 기준을 서술하시오.

---

### 해설

#### 1) 개념 정의

| 구분 | 식별자 관계 | 비식별자 관계 |
|------|-----------|-------------|
| **정의** | 부모 엔터티의 PK가 자식 엔터티의 **PK에 포함**되는 관계 | 부모 엔터티의 PK가 자식 엔터티의 **일반 속성(FK)** 으로 포함되는 관계 |
| **ERD 표기** | 실선(Solid Line) | 점선(Dashed Line) |
| **자식 독립성** | 부모 없이 자식 존재 불가 | 부모 없이 자식 존재 가능 |
| **PK 구조** | 복합 PK (부모PK + 자식고유속성) | 독립적 PK + FK |

#### 2) 식별자 관계의 장단점

**장점:**
- 데이터 무결성이 강력하게 보장됨 (부모 없는 자식 불가)
- 조인 시 FK 명시가 필요 없는 경우 쿼리 단순화

**단점:**
- 주식별자가 복합키로 구성되어 점점 길어지는 **PK 전파 문제** 발생
- 모델 변경 시 영향 범위가 넓어짐 (하위 엔터티 전체 수정 필요)
- 인덱스 크기 증가로 성능 저하 가능

#### 3) 비식별자 관계의 장단점

**장점:**
- 자식 엔터티의 PK가 단순하고 독립적 → 유지보수 용이
- 부모가 삭제되어도 자식 데이터 보존 가능 (이력 관리 등)

**단점:**
- NULL 허용 시 참조 무결성 약화 가능
- 조인 시 FK 컬럼을 별도로 관리해야 함

#### 4) 실무 적용 기준

| 상황 | 권장 관계 유형 |
|------|-------------|
| 자식이 부모 없이 절대 존재 불가 | 식별자 관계 |
| 계층이 3단계 이상 깊어질 경우 | 비식별자 관계 (PK 전파 방지) |
| 이력 데이터, 삭제된 부모 참조 필요 | 비식별자 관계 |
| 단순 코드성 참조 | 비식별자 관계 |

> **실무 권고**: 최근 트렌드는 **비식별자 관계를 기본으로 채택**하고, 업무적으로 명확한 종속 관계에만 선택적으로 식별자 관계를 적용하는 방향이 일반적이다.

---

## 문제 6. 데이터 웨어하우스 구축 방법론 비교

**[문제]**  
Kimball 방법론(Bottom-Up)과 Inmon 방법론(Top-Down)을 비교하고, 각 방법론의 핵심 아키텍처 구성 요소 및 실무에서 선택 기준을 설명하시오.

---

### 해설

#### 1) 방법론 비교

| 구분 | Kimball (Bottom-Up) | Inmon (Top-Down) |
|------|-------------------|-----------------|
| **접근 방식** | 데이터 마트(Data Mart) 먼저 구축 | 중앙 EDW(Enterprise DW) 먼저 구축 |
| **설계 중심** | 차원 모델링 (Star Schema, Snowflake) | 정규화된 3NF 모델 |
| **구현 속도** | 빠름 (단기 성과 가능) | 느림 (초기 투자 큼) |
| **확장성** | 데이터 마트 간 일관성 유지 어려움 | 중앙 집중으로 일관성 높음 |
| **사용자 편의성** | 높음 (비즈니스 중심 스키마) | 낮음 (복잡한 조인 필요) |
| **비용** | 초기 비용 낮음 | 초기 비용 높음 |

#### 2) Kimball 아키텍처 핵심 구성

```
Source Systems
    ↓
Staging Area (임시 저장)
    ↓
Integration Layer (통합 변환)
    ↓
[Data Mart 1] [Data Mart 2] [Data Mart 3]
(Star Schema / Snowflake Schema)
    ↓
BI / Reporting Tools
```

**핵심 개념:**
- **사실 테이블(Fact Table)**: 측정값(매출금액, 수량 등) 저장
- **차원 테이블(Dimension Table)**: 분석 기준(시간, 제품, 고객 등) 저장
- **버스 아키텍처(Bus Architecture)**: 공유 차원을 통해 데이터 마트 간 일관성 확보

#### 3) Inmon 아키텍처 핵심 구성

```
Source Systems
    ↓
ETL
    ↓
EDW (Enterprise Data Warehouse) - 3NF 정규화
    ↓
Data Mart (요약/집계 데이터)
    ↓
BI / Reporting Tools
```

#### 4) 선택 기준

| 상황 | 권장 방법론 |
|------|----------|
| 단기간 내 가시적 성과 필요 | Kimball |
| 전사적 데이터 일관성이 최우선 | Inmon |
| 소규모 조직, 특정 부서 분석 | Kimball |
| 대형 금융/공공기관, 규제 준수 | Inmon |
| 예산 제약이 큰 경우 | Kimball |

---

## 문제 7. Oracle SGA 구조와 성능 튜닝

**[문제]**  
Oracle Database의 SGA(System Global Area) 주요 구성 요소를 설명하고, Buffer Cache Hit Ratio와 Shared Pool의 Library Cache Hit Ratio를 활용한 메모리 성능 진단 방법과 튜닝 방안을 서술하시오.

---

### 해설

#### 1) SGA 주요 구성 요소

| 구성 요소 | 설명 | 주요 파라미터 |
|----------|------|------------|
| **Buffer Cache** | 데이터 블록을 메모리에 캐싱 | `DB_CACHE_SIZE` |
| **Shared Pool** | SQL 파싱 결과, 데이터 딕셔너리 캐시 | `SHARED_POOL_SIZE` |
| **Redo Log Buffer** | 변경 이력(Redo)을 임시 저장 | `LOG_BUFFER` |
| **Large Pool** | 백업, 병렬 쿼리 등 대용량 메모리 | `LARGE_POOL_SIZE` |
| **Java Pool** | Java 객체 저장 | `JAVA_POOL_SIZE` |
| **Streams Pool** | Oracle Streams 데이터 공유 | `STREAMS_POOL_SIZE` |

#### 2) Buffer Cache Hit Ratio

**공식:**
```
Buffer Cache Hit Ratio (%) = 
  (1 - Physical Reads / (Consistent Gets + DB Block Gets)) × 100
```

**조회 쿼리:**
```sql
SELECT 
  (1 - (phyrds.value / (congets.value + dbgets.value))) * 100 AS hit_ratio
FROM 
  v$sysstat phyrds, v$sysstat congets, v$sysstat dbgets
WHERE 
  phyrds.name = 'physical reads'
  AND congets.name = 'consistent gets'
  AND dbgets.name = 'db block gets';
```

**판단 기준:**

| Hit Ratio | 상태 | 조치 |
|-----------|------|------|
| 95% 이상 | 정상 | 현상 유지 |
| 90~95% | 주의 | 모니터링 강화 |
| 90% 미만 | 문제 | `DB_CACHE_SIZE` 증가 검토 |

#### 3) Shared Pool - Library Cache Hit Ratio

**공식:**
```
Library Cache Hit Ratio (%) = 
  SUM(PINHITS) / SUM(PINS) × 100
```

**조회 쿼리:**
```sql
SELECT 
  namespace,
  pins,
  pinhits,
  ROUND(pinhits/pins * 100, 2) AS hit_ratio,
  reloads,
  invalidations
FROM v$librarycache
WHERE pins > 0;
```

**낮은 Hit Ratio 원인 및 조치:**

| 원인 | 조치 |
|------|------|
| 하드 파싱(Hard Parse) 과다 | 바인드 변수(Bind Variable) 사용 유도 |
| `SHARED_POOL_SIZE` 부족 | 파라미터 증가 |
| 동적 SQL 남용 | Cursor 재사용 설계 |
| `cursor_sharing=FORCE` 미설정 | 파라미터 조정 검토 |

#### 4) AMM/ASMM을 통한 자동 메모리 관리
- **ASMM (Automatic Shared Memory Management)**: `SGA_TARGET` 설정으로 SGA 내 자동 배분
- **AMM (Automatic Memory Management)**: `MEMORY_TARGET` 설정으로 SGA + PGA 통합 자동 관리

---

## 문제 8. 엔터티 정의와 분류

**[문제]**  
데이터 모델링에서 엔터티(Entity)의 정의 요건을 설명하고, 엔터티를 발생 시점과 유형에 따라 분류하여 각각의 특성과 예시를 서술하시오.

---

### 해설

#### 1) 엔터티의 정의 요건

엔터티는 다음 **6가지 요건**을 모두 충족해야 한다:

| 요건 | 설명 |
|------|------|
| **업무에서 사용** | 실제 업무에서 관리되는 정보여야 함 |
| **식별 가능성** | 유일한 식별자(Identifier)로 구별 가능해야 함 |
| **2개 이상의 인스턴스** | 단 하나뿐인 값은 엔터티가 될 수 없음 |
| **속성 보유** | 엔터티를 설명하는 속성이 2개 이상 존재해야 함 |
| **관계 보유** | 다른 엔터티와 최소 1개 이상의 관계를 가져야 함 |
| **업무 프로세스 관리** | 업무 프로세스에 의해 생성/수정/삭제되는 정보여야 함 |

#### 2) 발생 시점에 따른 분류

| 분류 | 정의 | 특성 | 예시 |
|------|------|------|------|
| **기본 엔터티 (Fundamental)** | 업무에서 독립적으로 존재하는 원천 엔터티 | 독립 존재, 부모 없음 | 고객, 상품, 직원 |
| **중심 엔터티 (Main)** | 기본 엔터티로부터 발생하며 업무의 중심이 되는 엔터티 | 기본 엔터티에 종속 | 주문, 계약, 구매 |
| **행위 엔터티 (Active)** | 2개 이상의 엔터티 간 행위나 이벤트로 생성되는 엔터티 | 이력, 로그성 데이터 | 주문이력, 접속로그 |

#### 3) 유형에 따른 분류

| 분류 | 정의 | 예시 |
|------|------|------|
| **유형 엔터티 (Tangible)** | 물리적으로 존재하는 개념 | 직원, 고객, 창고 |
| **개념 엔터티 (Conceptual)** | 물리적으로 없는 개념적 정보 | 조직, 보험상품, 계정과목 |
| **사건 엔터티 (Event)** | 업무 수행 과정에서 발생하는 정보 | 주문, 이체, 청구 |

---

## 문제 9. 데이터 품질 관리

**[문제]**  
데이터 품질의 주요 측정 차원(Dimension)을 설명하고, 데이터 품질 관리 체계(DQM) 구축을 위한 절차와 데이터 프로파일링(Data Profiling)의 역할을 서술하시오.

---

### 해설

#### 1) 데이터 품질 측정 차원 (6대 품질 기준)

| 품질 차원 | 정의 | 측정 예시 |
|----------|------|---------|
| **정확성 (Accuracy)** | 실세계 값과 데이터 값이 일치하는 정도 | 주민번호와 실제 생년월일 일치 여부 |
| **완전성 (Completeness)** | 필수 데이터가 누락 없이 저장된 정도 | NULL 비율, 필수 항목 공백 비율 |
| **일관성 (Consistency)** | 다른 시스템/테이블 간 데이터가 동일한 정도 | 시스템 간 고객 주소 일치율 |
| **유효성 (Validity)** | 정의된 도메인, 코드, 형식을 준수하는 정도 | 날짜 형식 오류, 허용 범위 초과 값 |
| **유일성 (Uniqueness)** | 중복 없이 유일하게 존재하는 정도 | PK 중복 건수, 고객 중복 비율 |
| **적시성 (Timeliness)** | 데이터가 필요한 시점에 최신 상태로 존재하는 정도 | 데이터 갱신 주기 준수 여부 |

#### 2) 데이터 품질 관리 체계(DQM) 구축 절차

```
1단계: 데이터 품질 현황 분석
   - 현행 데이터 목록 및 소유자 파악
   - 비즈니스 임팩트 분석

2단계: 품질 기준 및 지표 정의
   - 품질 차원별 측정 지표(KPI) 설정
   - 목표 품질 수준(SLA) 정의

3단계: 데이터 프로파일링 수행
   - 실제 데이터 분석 (NULL, 분포, 중복 등)
   - 품질 문제 항목 식별

4단계: 품질 개선 계획 수립
   - 원인 분석 (Root Cause Analysis)
   - 개선 우선순위 결정

5단계: 품질 개선 실행
   - 데이터 정제(Cleansing) 수행
   - 프로세스/시스템 개선

6단계: 모니터링 및 지속 관리
   - 품질 측정 자동화
   - 주기적 품질 리포팅
```

#### 3) 데이터 프로파일링(Data Profiling)의 역할

데이터 프로파일링이란 **데이터의 실제 내용, 구조, 품질을 자동으로 분석**하는 과정이다.

**주요 분석 유형:**

| 분석 유형 | 내용 |
|----------|------|
| **컬럼 프로파일링** | NULL 비율, 최솟값/최댓값, 평균, 고유값 수 |
| **의존성 분석** | 컬럼 간 함수 종속 관계 탐지 |
| **중복 분석** | 레코드 및 컬럼 수준 중복 탐지 |
| **형식 분석** | 날짜, 전화번호 등 형식 일관성 검사 |
| **참조 무결성 분석** | FK 참조 오류, 고아 레코드 탐지 |

---

## 문제 10. 마스터 데이터 관리(MDM)

**[문제]**  
MDM(Master Data Management)의 개념과 필요성을 설명하고, MDM 구현 아키텍처 유형(Registry, Consolidation, Coexistence, Centralized)을 비교하며, 마스터 데이터 거버넌스와의 관계를 서술하시오.

---

### 해설

#### 1) MDM의 개념과 필요성

**MDM(Master Data Management)** 이란 기업의 핵심 공통 참조 데이터(고객, 상품, 공급업체, 조직 등)를 **단일하고 신뢰 가능한 버전(Single Version of Truth)** 으로 관리하는 체계이다.

**필요성:**
- 여러 시스템에 분산된 마스터 데이터의 불일치로 인한 **의사결정 오류 방지**
- M&A, 시스템 통합 시 **데이터 통합 비용 절감**
- 금융·의료 등 **규제 준수(Compliance)** 요건 충족
- **CRM, ERP, SCM 간 데이터 정합성** 확보

#### 2) MDM 구현 아키텍처 4가지 유형

| 유형 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **Registry** | 각 소스시스템의 데이터를 그대로 두고, 중앙 인덱스(Registry)로 연결 | 기존 시스템 변경 최소화 | 실시간 정합성 보장 어려움 |
| **Consolidation** | 소스 시스템 데이터를 MDM Hub에 통합 저장, 소스 시스템은 독립 유지 | 분석·리포팅 용이 | 소스 시스템으로의 역방향 배포 없음 |
| **Coexistence** | MDM Hub와 소스 시스템이 양방향으로 데이터를 공유 | 높은 유연성 | 동기화 복잡도 증가 |
| **Centralized** | MDM Hub가 유일한 마스터 데이터 저장소, 소스 시스템은 Hub에서 조회 | 가장 강력한 일관성 보장 | 구현 비용 및 영향 범위 최대 |

#### 3) MDM 아키텍처 선택 기준

| 상황 | 권장 아키텍처 |
|------|------------|
| 초기 구축, 소스 시스템 변경 불가 | Registry |
| BI/분석 목적, 소스 독립성 유지 | Consolidation |
| 다수 시스템 양방향 동기화 필요 | Coexistence |
| 완전한 데이터 통제, 신규 구축 | Centralized |

#### 4) 마스터 데이터 거버넌스와의 관계

MDM은 **기술적 구현**이고, **데이터 거버넌스(Data Governance)** 는 이를 뒷받침하는 **조직·정책·프로세스** 체계이다.

```
데이터 거버넌스
├── 데이터 오너십 정의 (Data Steward 지정)
├── 데이터 정책 및 표준 수립
├── 데이터 품질 기준 정의
└── 변경 관리 프로세스
         ↓ 실행 도구
    MDM 시스템
├── 마스터 데이터 통합 관리
├── 중복 제거 및 정제
├── 소스 시스템 연계
└── 품질 모니터링
```

**핵심 역할 구분:**
- **Data Steward**: 마스터 데이터의 품질과 정확성에 책임을 지는 업무 담당자
- **Data Owner**: 특정 도메인 마스터 데이터의 최종 책임자 (임원급)
- **DBA/DA**: 기술적 구현 및 시스템 관리 담당

> MDM의 성공은 기술보다 **거버넌스와 조직 변화 관리(Change Management)** 에 달려 있다고 해도 과언이 아니다. 아무리 좋은 MDM 시스템을 도입해도 데이터 오너십과 거버넌스 체계가 미흡하면 실패한다.

---

## 📌 최종 점검 체크리스트

| 번호 | 주제 | 핵심 키워드 |
|------|------|-----------|
| 1 | 정규화/역정규화 | 갱신 이상, 트리거 동기화, 성능 vs 무결성 |
| 2 | BCNF 분해 | 결정자, 후보키, 무손실 조인, 의존성 보존 |
| 3 | 데이터 표준화 | 표준 단어/도메인/코드/용어, 상호 운용성 |
| 4 | 모델링 3단계 | 개념/논리/물리, 산출물, DBMS 독립성 |
| 5 | 식별자 관계 | PK 전파 문제, 실선/점선, 종속성 |
| 6 | DW 방법론 | Kimball vs Inmon, Star Schema, Bus Architecture |
| 7 | SGA 튜닝 | Buffer Cache Hit Ratio, Library Cache, 바인드 변수 |
| 8 | 엔터티 분류 | 기본/중심/행위, 유형/개념/사건 |
| 9 | 데이터 품질 | 6대 품질 차원, 프로파일링, DQM |
| 10 | MDM | 4가지 아키텍처, Data Steward, 거버넌스 |
