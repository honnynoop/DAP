# DAP 4과목 데이터 모델링 완전 정리

> 출처: https://lumasca.tistory.com/1179  
> ※ 이미지는 위 블로그에서 직접 다운로드 필요 (우클릭 → 이미지 저장)

---

## 목차

- [제1장 데이터 모델링 이해](#제1장-데이터-모델링-이해)
  - [1절 데이터 모델링 개요](#1절-데이터-모델링-개요)
  - [2절 데이터 모델링 기법 이해](#2절-데이터-모델링-기법-이해)
  - [3절 데이터 모델링 표기법 이해](#3절-데이터-모델링-표기법-이해)
  - [4절 관계형 모델 이론](#4절-관계형-모델-이론)
- [제2장 개념 데이터 모델링](#제2장-개념-데이터-모델링)
  - [1절 개념 데이터 모델링 이해](#1절-개념-데이터-모델링-이해)
  - [2절 주제 영역 정의](#2절-주제-영역-정의)
  - [3절 후보 엔터티 선정](#3절-후보-엔터티-선정)
  - [4절 핵심 엔터티 정의](#4절-핵심-엔터티-정의)
  - [5절 관계 정의](#5절-관계-정의)
  - [6절 개념 데이터 모델 작성](#6절-개념-데이터-모델-작성)
- [제3장 논리 데이터 모델링](#제3장-논리-데이터-모델링)
  - [1절 논리 데이터 모델링 이해](#1절-논리-데이터-모델링-이해)
  - [2절 속성 정의](#2절-속성-정의)
  - [3절 엔터티 상세화](#3절-엔터티-상세화)
  - [4절 이력관리 정의](#4절-이력관리-정의)
  - [5절 논리 데이터 모델 품질 검토](#5절-논리-데이터-모델-품질-검토)
- [제4장 물리 데이터 모델링](#제4장-물리-데이터-모델링)
  - [1절 물리 데이터 모델링 이해](#1절-물리-데이터-모델링-이해)
  - [2절 물리 요소 조사 및 분석](#2절-물리-요소-조사-및-분석)
  - [3절 논리-물리 모델 변환](#3절-논리-물리-모델-변환)
  - [4절 반정규화](#4절-반정규화)
  - [5절 물리 데이터 모델 품질 검토](#5절-물리-데이터-모델-품질-검토)

---

# 제1장 데이터 모델링 이해

## 1절 데이터 모델링 개요

### 1. 데이터 모델링 정의

#### 가. 모델 정의

| 구분 | 설명 | 예시 |
|------|------|------|
| **물리적 모델** | 현실에 실재하는 복잡한 대상의 축소/재현 | 자동차 모형, 선박 설계도, 바람터널 축소 모형 |
| **개념적 모델** | 추상적·수리적 공식으로 현실을 표현 | 기상 예측 수리 공식, 경제 모형 |
| **데이터 모델** | 관심 대상을 DB화하기 위한 개념적 도구 | ERD, 논리 데이터 모델 |

#### 나. 데이터 모델링 정의
- 사용자의 **요구사항**으로부터 데이터의 실체를 나타내는 일
- 기업 업무 규칙에 대해 참/거짓을 판별할 수 있는 사실을 **명확하게 표현하는 추상화 기법**
- 이에 대한 전산화와는 **별개의 관점**에서 표현

#### 다. 데이터 모델이 제공하는 것
- 시스템을 현재 또는 원하는 모습으로 **가시화**
- 시스템의 구조와 행동을 **명세화**
- 시스템을 구축하는 **틀 제공**
- 우리가 결정한 것을 **문서화**
- 다양한 영역에 집중하기 위해 다른 영역의 세부 사항은 숨기는 **다양한 관점 제공**
- 특정 목표에 따라 **다양한 상세 수준 제공**

#### 라. 본질적 데이터 요구사항

| 항목 | 설명 |
|------|------|
| **이름(Name)** | 모든 속성은 고유하게 식별할 수 있는 이름 필요 |
| **명세(Description)** | 모든 실체는 명세가 있어야 하며, 누구든지 정확히 해석 가능해야 함 |
| **유형(Type)** | 속성은 Key 속성 또는 Non-Key 속성 중 하나로 구분 |

---

### 2. 데이터 모델링의 중요성

#### 가. 3대 문제점 (데이터 모델링 시 주의점)

| 문제점 | 설명 | 해결 방향 |
|--------|------|-----------|
| **중복(Duplication)** | 같은 데이터가 여러 곳에 저장 | 중복 저장 제거 |
| **비유연성(Inflexibility)** | 프로세스 변화에 과도하게 종속 | 프로세스와 분리하여 설계 |
| **비일관성(Inconsistency)** | 데이터 간 상호관계 모순 발생 | 명확한 관계 정의로 모순 제거 |

#### 나. 중요성 3가지 관점

| 관점 | 설명 |
|------|------|
| **파급효과(Leverage)** | 완성 시점에서 모델 변경 시 전체 시스템에 큰 영향 |
| **간결한 표현** | 정보 요구사항과 한계를 가장 명확하고 간결하게 표현 |
| **데이터 품질** | 낮은 품질의 데이터는 활용 가치 저하 |

---

### 3. 좋은 데이터 모델의 요소 ★ (완중업재 안의간통)

| 순서 | 요소 | 설명 |
|------|------|------|
| 1 | **완전성** | 업무에서 필요로 하는 모든 데이터가 모델에 정의 |
| 2 | **중복배제** | 하나의 DB 내에 동일한 사실은 반드시 한 번만 기록 |
| 3 | **비즈니스 룰** | 업무 규칙을 데이터 모델에 표현, 모든 사용자가 공유 |
| 4 | **데이터 재사용** | 통합성과 독립성 고려, 애플리케이션에 독립적 설계 |
| 5 | **안정성·확장성** | 구조 변화 최소화하면서 변화에 대응 가능 |
| 6 | **간결성** | 합리적이고 균형 있게 단순하게 분류 |
| 7 | **의사소통** | 관련자들이 동일한 의미로 업무 규칙 공유 |
| 8 | **통합성** | 전체 조직에서 한 번만 정의, 여러 영역에서 참조 |

---

### 4. 데이터 모델링 단계

| 단계 | 목적 | 산출물 | 주요 활동 |
|------|------|--------|-----------|
| **개념 모델링** | 업무 핵심 구조 파악 | 개념 ERD | 핵심 엔터티 추출, 관계 정의 |
| **논리 모델링** | 비즈니스 규칙 명확화 | 논리 ERD | 속성 정의, 식별자 확정, 정규화, M:M 해소, 참조무결성 |
| **물리 모델링** | DB 구현 최적화 | 물리 스키마 | DBMS 특성 고려, 데이터 타입 정의, 반정규화, 인덱스 정의 |

---

## 2절 데이터 모델링 기법 이해

### 1. 엔터티-관계 데이터 모델

#### 가. 추상화 기법

| 기법 | 설명 | 예시 |
|------|------|------|
| **유형화** | 같은 성질의 멤버들을 타입/유형이라는 하나의 개념으로 정의 | 고객 타입 정의 |
| **집단화** | 값을 유형화하면 속성이라는 타입이 됨 | 속성 그룹화 |
| **일반화** | 여러 엔터티 타입 간의 공통적인 특성 파악 | 슈퍼타입·서브타입 |

#### 나. 타입과 인스턴스

| 구분 | 설명 |
|------|------|
| **타입(Type)** | 실제 발생된 사례들을 추상화한 집합 (= 엔터티) |
| **인스턴스(Instance)** | 현실세계에서 실제로 발생된 사례 (타입의 멤버, = 행/Row) |

> 📌 이미지 위치: 타입과 인스턴스 개념도 [블로그 참조]

---

### 2. ER 모델 구성요소

| 구성요소 | 설명 | 특징 |
|----------|------|------|
| **엔터티** | 지속적 관심을 가져야 하는 대상의 개체 집합 | 동질성을 지닌 멤버들의 집합체 |
| **관계** | 두 엔터티 간 업무적 연관성 | 관계도 집합, 외래키로 구현 |
| **속성** | 엔터티에 저장되는 인스턴스의 특성 | 원자값, 식별자에만 종속 |
| **도메인** | 속성이 가질 수 있는 모든 값의 집합 | 데이터 타입·길이·허용값 |
| **식별자** | 특정 인스턴스 하나를 유일하게 식별 | Not Null, 유일성, 대표성, 최소성 |
| **카디날리티** | 관계에 참여하는 엔터티 개수 (1:1, 1:M, M:M) | 최대/최소 대응수 |
| **존재종속** | 한 엔터티의 존재가 다른 엔터티 존재에 영향 | 전체참여, 부분참여 |
| **서브타입** | 엔터티의 하위 집합, 속성 상속 | 배타적/포괄적 |

---

### 3. 카디날리티 유형

| 유형 | 설명 | 예시 |
|------|------|------|
| **1:1** | X의 한 개체 ↔ Y의 한 개체 | 사원 - 사원증 |
| **1:M** | X의 한 개체 → Y의 여러 개체 | 부서 - 사원 |
| **M:M** | X의 여러 개체 ↔ Y의 여러 개체 | 학생 - 수업 |

---

### 4. 서브타입 유형

| 유형 | 설명 | 표기 | 예시 |
|------|------|------|------|
| **배타적(Exclusive)** | 슈퍼타입은 **최대 1개** 서브타입과 관련 | 돔 안에 X 표시 | 사원 = 정규직 OR 계약직 |
| **포괄적(Inclusive)** | 슈퍼타입은 **1개 이상** 서브타입과 관련 | 돔 안에 선 | 연예인 = 가수 AND/OR 배우 |

---

### 5. 객체지향 모델링 vs 논리 데이터 모델링 대응 관계

| 객체지향 | 논리 데이터 모델링 |
|----------|------------------|
| 객체(Object) | 엔터티(Entity) |
| 속성(Attribute) | 속성(Attribute) |
| 연결(Association) | 관계(Relationship) |
| 객체 클래스(Object Class) | 엔터티 유형(Entity Type) |

---

## 3절 데이터 모델링 표기법 이해

### 가. 바커(Barker) 표기법

#### 1) 엔터티
- 네 모서리가 둥근 **소프트-박스(Soft-box)** 로 표현
- 실체명은 박스 **안에** 작성

> 📌 이미지: 바커 표기법 엔터티 예시 [블로그 참조]

#### 2) 속성 표기

| 기호 | 의미 |
|------|------|
| `*` | Mandatory (필수 입력) |
| `o` | Optional (선택 입력) |
| `#` | 식별자(Identifier) |

> 📌 이미지: 바커 표기법 속성 예시 [블로그 참조]

#### 3) 관계 표기

| 구분 | 표기 방법 |
|------|-----------|
| **필수 관계** | 실선(Solid line) |
| **선택 관계** | 점선(Dashed line) |
| **1(단 하나)** | 수평선(`|`) |
| **M(하나 이상)** | 까마귀 발가락(Crow's Foot) |

> 📌 이미지: 바커 관계 표기법 예시 [블로그 참조]  
> 📌 이미지: 1:1, 1:M, M:M 관계 표기 예시 [블로그 참조]

#### 4) 관계 선택성

| 구분 | 조건 | 표기 |
|------|------|------|
| **필수(Mandatory)** | 상대 엔터티에 반드시 한 건 이상 존재해야 함 | 실선 |
| **선택(Optional)** | 상대 엔터티에 행이 존재하지 않아도 됨 | 점선 |

#### 5) 관계 식별성

| 구분 | 설명 | 예시 |
|------|------|------|
| **식별관계** | 부모 식별자 → 자식 **식별자의 일부** | 주문-주문상품 |
| **비식별관계** | 부모 식별자 → 자식 **일반 속성** | 부서-사원 |

> 📌 이미지: 식별/비식별 관계 표기 예시 [블로그 참조]

#### 6) 식별자 유형

| 유형 | 설명 |
|------|------|
| **본질 식별자** | 집합의 본질을 명확히 설명하는 의미상의 주어 |
| **후보 식별자** | 각 인스턴스를 유일하게 식별 가능한 속성 또는 조합 |
| **대체(보조) 식별자** | 원래 식별자를 대신할 수 있는 또 다른 속성/관계 |
| **인조 식별자** | 본질 식별자를 임의의 값으로 대체하여 새롭게 구성 |
| **실질 식별자** | 최종 확정된 공식 식별자 (본질 또는 인조) |

> 📌 이미지: 식별자 표기 예시 [블로그 참조]

#### 7) 서브타입 표기

> 📌 이미지: 바커 서브타입 표기 예시 [블로그 참조]

---

### 나. IE 표기법 (정보공학, Information Engineering)

#### 1) 엔터티
- **사각 박스**로 표현, 엔터티명은 박스 **밖**에 기재

> 📌 이미지: IE 표기법 엔터티 예시 [블로그 참조]

#### 2) 관계 표기 비교 (바커 vs IE)

| 구분 | 바커(Barker) | IE(정보공학) |
|------|-------------|-------------|
| 엔터티 모양 | 소프트-박스(모서리 둥글게) | 사각 박스 |
| 엔터티명 위치 | 박스 **안** | 박스 **밖** |
| 필수 표기 | 실선 | 세로선(`|`) |
| 선택 표기 | 점선 | `o` |
| 식별자 표기 | `#` 기호 | 수평선(식별자 아래) |
| M 표기 | 까마귀 발가락 | 까마귀 발가락 |

> 📌 이미지: IE 표기법 관계 예시 [블로그 참조]

#### 3) IE 서브타입

| 구분 | 설명 |
|------|------|
| **배타적(Exclusive)** | 동시에 1개 서브타입만 가능 (예: 사원 = 정규직 OR 계약직) |
| **포괄적(Inclusive)** | 동시에 1개 이상 서브타입 가능 (예: 연예인 = 가수 AND/OR 배우) |

> 📌 이미지: IE 서브타입 표기 예시 [블로그 참조]  
> 📌 이미지: 바커 vs IE 관계 표현 비교 [블로그 참조]

---

## 4절 관계형 모델 이론

### 1. 릴레이션의 6가지 특성

| 번호 | 특성 | 설명 |
|------|------|------|
| 1 | **원자성** | 각 열은 하나의 값(원자값) |
| 2 | **도메인 무결성** | 각 열의 값은 동일한 종류 |
| 3 | **튜플 유일성** | 각 행은 유일 → 엔터티 무결성 |
| 4 | **애트리뷰트 무순서성** | 열의 순서는 무의미 |
| 5 | **튜플 무순서성** | 행의 순서는 무의미 |
| 6 | **열 이름 유일성** | 각 열은 유일한 이름 |

---

### 2. 관계 연산자

#### 조회 연산자

| 연산자 | 기호 | 설명 | SQL 예시 |
|--------|------|------|----------|
| **Select (Restrict)** | σ | 조건에 맞는 행의 부분집합 | `WHERE Salary > 50000` |
| **Project** | π | 열의 부분집합 | `SELECT col1, col2` |
| **Product** | × | 두 테이블의 행 조합 (카티션) | `FROM A, B` |
| **Join** | ⨝ | 열 기준으로 행을 수평 결합 | `INNER JOIN ON` |
| **Union** | ∪ | 중복 없이 행을 수직 결합 (합집합) | `UNION` |
| **Intersection** | ∩ | 공통된 행 (교집합) | `INTERSECT` |
| **Difference** | − | 하나에만 있는 행 (차집합) | `EXCEPT` |
| **Division** | ÷ | 다른 테이블 모든 행에 대응하는 행 | `IN (SELECT ...)` |

#### 처리 연산자

| 연산자 | 설명 |
|--------|------|
| **Insert** | 행의 삽입 |
| **Update** | 행의 수정 |
| **Delete** | 행의 삭제 |

---

### 3. 데이터 무결성

| 유형 | 설명 | 핵심 규칙 |
|------|------|-----------|
| **엔터티 무결성** | 동일 튜플 불허, PK는 NOT NULL + 유일 | 기본키(PK) 제약 |
| **참조 무결성** | 외래키 값은 부모 테이블 PK에 존재해야 함 | FK 제약 |
| **도메인 무결성** | 데이터 타입·길이·허용값·NULL 여부 준수 | 컬럼 제약 |
| **연쇄작용** | 입력/수정/삭제가 다른 엔터티에 영향 | 업무 규칙 |

#### 참조무결성 입력 규칙

| 규칙 | 설명 |
|------|------|
| **DEPENDENT** | 부모 존재 시에만 자식 입력 허용 |
| **AUTOMATIC** | 자식 입력 허용, 부모 없으면 자동 생성 |
| **NULLIFY** | 자식 입력 허용, 부모 없으면 FK를 NULL로 |
| **DEFAULT** | 자식 입력 허용, 부모 없으면 FK를 기본값으로 |
| **CUSTOMIZED** | 특정 검증 조건 만족 시만 허용 |
| **NO EFFECT** | 조건 없이 허용 |

#### 참조무결성 삭제 규칙

| 규칙 | 설명 |
|------|------|
| **RESTRICT** | 자식 없는 경우에만 부모 삭제 허용 |
| **CASCADE** | 부모 삭제 시 자식도 자동 삭제 |
| **NULLIFY** | 부모 삭제 시 자식 FK를 NULL로 |
| **DEFAULT** | 부모 삭제 시 자식 FK를 기본값으로 |
| **CUSTOMIZED** | 특정 검증 조건 만족 시만 허용 |
| **NO EFFECT** | 조건 없이 부모 삭제 허용 |

---

### 4. 관계형 vs 비관계형 비교

| 구분 | 관계형 | 비관계형 |
|------|--------|----------|
| **구조** | 6가지 특성의 테이블, Post-Define Schema, DB 구조 변경 용이 | Flat File, Pre-define Schema, DB 구조 변경 어려움 |
| **조작** | 8개 조회/처리 연산자, 집합(SET) 처리, 원자값 처리 | READ 조회, 처리 연산자, 한 건씩 처리, Pointer 처리 |
| **무결성** | 엔터티·참조·도메인·연쇄작용 | 무결성 개념 없음 |
| **접근경로** | Key와 인덱스 별개, 접근경로 유연 | Key와 인덱스 동일, 접근경로 제한적 |

---

# 제2장 개념 데이터 모델링

## 1절 개념 데이터 모델링 이해

### 가. 개념 데이터 모델 정의
- **핵심 엔터티**들로 구성된 모델
- 핵심 엔터티: 행위의 주체나 목적물이 되는 **개체 집합**에 해당
- 개괄 데이터 모델과 논리 데이터 모델 **사이에 위치**

### 나. 개념 모델링 구성요소 (순서)

```
주제영역 → 핵심엔터티 → 관계 → 핵심속성 → 식별자
```

### 다. 데이터 아키텍처 레벨

| 레벨 | 모델 | 특징 |
|------|------|------|
| 최상위 | **개괄 데이터 모델** | 전체 조직 최상위 수준 |
| 중간 | **개념 데이터 모델** | 핵심 엔터티·관계 정의 |
| 상세 | **논리 데이터 모델** | 속성·식별자·정규화 |
| 구현 | **물리 데이터 모델** | DBMS 특성 반영 |

---

## 2절 주제 영역 정의

### 가. 주제 영역 개념
- 기업이 사용하는 데이터의 **최상위 집합** (예: 인사, 생산, 자재, 판매)
- 주제영역 내부 개체들이 **높은 결합성** 유지
- 프로세스 모델링의 **기능과 매핑**

### 나. 주제 영역 분류 원칙

| 원칙 | 설명 |
|------|------|
| **데이터 중복 최소화** | 동일 유형 데이터는 동일 영역으로 |
| **데이터 확장성 보장** | 업무 요건 변경 시 구조 변경 최소화 |
| **관련성·편의성 확보** | 타 자원과의 인접성, 고객 편의 고려 |
| **주제영역 간 균형 유지** | 전체적인 균형 유지, 편중 분류 지양 |

### 다. 주제 영역 명명 규칙
- 실 업무에서 보편적으로 사용하는 **업무 용어**
- **유일한 단수형 명사**
- **데이터의 그룹**을 의미하는 이름 (업무 활동명 배제)
- 예) `재무관리(X)` → `재무분야(O)`

### 라. 주제 영역 분류 방법 (3단계)

| 분류 | 기준 | 예시 |
|------|------|------|
| **1차 분류** | 주요 데이터 집합 유형 정의 | 관계자, 상품/서비스, 자산, 채널, 계약 |
| **2차 분류** | Biz 활동에 필요한 데이터 분류 | 관계자 기본, 관계자 상세, 관계자 관계 |
| **3차 분류** | 2차 영역의 세부 주제 분류 | 관계자 기본 → 고객, 법인, 조직, 직원 |

### 마. 주제 영역 도출 방법

| 방법 | 설명 |
|------|------|
| **하향식** | 주제 영역에서 출발하여 엔터티 타입으로 전개 |
| **상향식** | 엔터티 타입을 그룹핑하여 주제 영역 도출 |
| **명사형 도출** | 업무에서 사용하는 데이터의 명사형 찾기 |
| **업무 기능 도출** | 업무 기능 이름으로부터 도출 |
| **분석 단계 도출** | 아키텍처 모델 정련 과정에서 도출 |

---

## 3절 후보 엔터티 선정

### 가. 엔터티 후보 식별 절차

```
엔터티 후보 선정 → 엔터티 형태별 분류 → 엔터티 자격 검증
→ 엔터티 형태 결정 → 본질식별자 확정 → 엔터티 정의서 작성
```

### 나. 엔터티 후보 수집처

| 수집처 | 설명 |
|--------|------|
| 기존 시스템 도큐먼트 | 설계 자료, 사용자 지침서 |
| 현업 장표/보고서 | 업무 처리 장표, 집계·분석 보고서 |
| 현업 인터뷰 | 현업 담당자 인터뷰 |
| 데이터 흐름도(DFD) | 데이터 저장소 및 데이터 사전 활용 |
| 타 시스템 자료 | 타 시스템 엔터티 면밀 조사 |
| 현장 조사 | 현장 답사 |
| 관련 전문 서적 | 아이디어·힌트 도출 |

### 다. 엔터티 후보 선정 시 유의사항
- 가능성 있으면 일단 검토 대상에 포함
- 너무 깊게 들어가지 말 것
- 동의어처럼 보여도 함부로 버리지 말 것
- 프로세스에 너무 연연하지 말 것
- 예외 경우에 집착하지 말 것
- 구현할 시스템의 **업의 본질** 염두

### 라. 집합 여부 확인 기준
- **가로** (열 방향): 속성 (2개 이상)
- **세로** (행 방향): 인스턴스 (2개 이상)

---

## 4절 핵심 엔터티 정의

### 가. 엔터티 분류

#### 1) 발생 시점 기준 분류

| 분류 | 설명 | 예시 |
|------|------|------|
| **키 엔터티** | 부모를 가지지 않는 엔터티 | 사원, 부서, 고객, 상품, 자재 |
| **메인 엔터티** | 업무의 중심 엔터티, 부모 필요 | 주문, 매출, 예산편성 |
| **액션 엔터티** | 키·메인이 아닌 엔터티, 반드시 부모 가짐 | 상태이력, 상세주문내역 |

#### 2) 일반적 분류

| 분류 | 설명 | 예시 |
|------|------|------|
| **유형 엔터티** | 물리적으로 존재하는 대상 | 고객, 상품 |
| **활동 엔터티** | 어떤 사건에 관한 정보 | 주문, 계약 |
| **개념 엔터티** | 관리할 정보 있는 무형의 개념 | 성적 |

#### 3) 모델 관점 분류

| 분류 | 설명 | 예시 |
|------|------|------|
| **독립 엔터티** | 다른 인스턴스에 의존하지 않음 | 고객, 제품, 사원 |
| **종속 엔터티** | 1개 이상 다른 인스턴스에 의존 | 사원가족, 주문상품 |
| ↳ 특성 엔터티 | 하나의 인스턴스에 여러 번 발생하는 속성 그룹 | 사원경력 |
| ↳ 연관 엔터티 | 두 개 이상 엔터티로부터 식별자 상속 (M:M 해소) | 학생수업 |
| ↳ 서브타입 엔터티 | 공통 속성/관계를 공유하는 엔터티의 부분집합 | 개인고객, 법인고객 |

---

### 나. 서브타입 관련

#### 서브타입 지정 기준

| 적용 기준 | 설명 |
|-----------|------|
| **분류 속성** | 분류 속성에 따라 엔터티 정보가 차별화 |
| **개별 속성** | 서브타입별로 고유 속성이 존재 |
| **개별 관계** | 서브타입별로 다른 관계 보유 |
| **가독성** | 데이터 모델 이해 향상 목적 |

#### 서브타입 규칙
- 교집합 허용 **불가** (남자/여자는 교집합 없음)
- 서브타입의 합이 **전체집합**
- 하나의 엔터티에 서브타입 유형은 **하나만** 존재 (최종 모델)
  - 단, 바커 표기법에서는 멀티 서브타입 표현 가능

#### 일반화(서브타입) 장단점

| 구분 | 일반화 (통합) | 상세화 (분리) |
|------|-------------|-------------|
| **장점** | 조회 간편, 단일 프로그램 처리, 업무 추가 시 최소 변경, 신속한 업무 로직 반영 | 업무 명확한 파악, 데이터 추가·변경 용이, 특화 속성 관리 용이, 장애 파급효과 작음 |
| **단점** | 공통 속성이 적으면 효과 반감, 데이터 혼재로 파악 어려움, 타 업무 영향 발생 | 엔터티 수 증가, 데이터 불일치 가능, 통합 정보 도출 어려움, 별도 프로그램 개발 필요 |

---

### 다. 엔터티 통합과 분할

| 원칙 | 설명 |
|------|------|
| **통합 권장** | 키 엔터티는 가능한 통합, 유연성 향상 |
| **무리한 통합 지양** | 집합의 독립성 저해 우려 |
| **수평 분할** | 데이터가 많고 특정 범위만 주로 접근 시 |
| **수직 분할** | 컬럼 수가 많고 조회/갱신 컬럼이 구별될 때 |

---

## 5절 관계 정의

### 가. 관계 형태

| 형태 | 기수성 | 선택성 | 예시 |
|------|--------|--------|------|
| **1:1 필수-필수** | 양쪽 1 | 양쪽 실선 | 고객-여권 |
| **1:1 필수-선택** | 양쪽 1 | 한쪽 실선, 한쪽 점선 | 학생-학생 프로파일 |
| **1:1 선택-선택** | 양쪽 1 | 양쪽 점선 | 직원-세부정보(수직분할) |
| **1:M 양쪽 필수** | 1:M | 양쪽 실선 | 주문-주문상품 |
| **1:M 1측 옵션** | 1:M | 1측 점선, M측 실선 | 교사-학급 |
| **1:M M측 옵션** | 1:M | 1측 실선, M측 점선 | 카테고리-책 |
| **M:M** | M:M | - | 학생-수업 (해소 필요) |

### 나. 특수한 형태의 관계

| 유형 | 설명 | 특징 |
|------|------|------|
| **순환 관계** | 엔터티가 자기 자신과 관계 | 반드시 선택 관계 (필수 관계 불가 - 무한루프) |
| **BOM 관계** | M:M 순환구조 (부품 구성도) | 관계 엔터티 추가로 두 개의 1:M 관계로 구성 |
| **ARC(배타적) 관계** | 둘 이상 엔터티 합집합과 관계 | 아크 내 관계는 동일 선택성, 하나의 엔터티에만 속함 |

### 다. 다중 관계 처리

| 방식 | 설명 | 특징 |
|------|------|------|
| **병렬식 관계** | 두 엔터티 간 관계를 별도 관계로 관리 | 테이블에서 여러 컬럼으로 나열, 인덱스 증가, 확장에 취약 |
| **직렬식 관계** | 여러 관계를 하나의 관계로 통합 관리 | 관리 엔터티 추가, 행(ROW)으로 관리, 인덱스 감소, 확장에 유연 |

---

## 6절 개념 데이터 모델 작성

### 작성 방법 (3가지 접근)

| 접근 방법 | 프로세스 |
|-----------|----------|
| **개괄 모델에서 상세화** | 개괄 모델 → 단위 주제영역 결정 → 핵심 엔터티 매핑 → 관계 정의 → 검증 |
| **엔터티 후보로부터 작성** | 후보 검토·분류 → 핵심 엔터티 도출 → 주제영역 정의 → 관계 정의 → 검증 |
| **현행 데이터 리버스** | 현행 물리 모델 → 논리화 → 핵심 엔터티 도출 → 현행 개념 모델 → 목표 개념 모델 |

---

# 제3장 논리 데이터 모델링

## 1절 논리 데이터 모델링 이해

### 가. 정의
- 비즈니스 정보의 구조와 규칙을 **명확하게 표현**하는 기법
- 현실세계를 **추상화·개념화**
- DB 설계 프로세스의 **Input**

### 나. 목적 및 효과

| 목적 | 설명 |
|------|------|
| 데이터 명확한 이해 | 비즈니스 데이터 관점에서 명확히 이해 |
| 전사적 통합 체계 | 전사 통합 데이터 체계 확립 |
| 일관성·정확성 | 데이터 일관성·정확성 유지 규칙 도출 |
| 안정적 DB 설계 | 논리 모델이 물리 설계의 토대 |
| 의사소통 수단 | 사용자와의 명확한 커뮤니케이션 |

### 다. 필수 성공 요소
- 업무에 능통한 **현업 사용자**와 함께 진행
- **절차**보다는 **데이터**에 초점
- 데이터 **구조와 무결성** 함께 고려
- **개념화와 정규화** 기법 적용
- 가능하면 **다이어그램** 이용
- **데이터 사전** 구축

---

## 2절 속성 정의

### 가. 속성 개념
- 엔터티에서 관리되는 구체적인 정보 항목
- **더 이상 분리될 수 없는 최소의 데이터 보관 단위**
- 속성도 일종의 집합, 속성들 간은 서로 독립적, **식별자에만 종속**

### 나. 속성 검증 기준

| 검증 항목 | 기준 | 비고 |
|-----------|------|------|
| **최소 단위 분할** | 더 이상 분해할 수 없는 원자값 | 일자·성명 등은 일반적으로 분할 불필요 |
| **하나의 값** | 반복되는 속성 → 1차 정규화 대상 | 다중값이면 새 엔터티로 분리 |
| **추출 속성 여부** | 다른 속성에 의해 계산·유도되는지 확인 | 원천값인지 파생값인지 구분 |

### 다. 속성 유형

| 유형 | 설명 | 예시 |
|------|------|------|
| **기본 속성(Basic)** | 해당 인스턴스에 원래 존재, 유도 불가 | 이름, 생년월일 |
| **설계 속성(Design)** | 업무 제약사항 반영·시스템 운영 단순화 | 코드값, 구분자 |
| **유도(파생) 속성(Derived)** | 다른 속성 값으로부터 계산·유도 | 나이(생년월일에서), 합계 |

### 라. 추출(유도) 속성 규칙
- 경영층이 원하는 정보를 대표
- **기본키가 되면 안됨**
- 데이터 모델에 반드시 기술
- 예: 최초가입일, 고객활동상태, 현주소

### 마. 속성 정의 시 유의사항

| 항목 | 내용 |
|------|------|
| 명칭 | 의미가 명확한 명사형 사용, 엔터티명 포함 권장 |
| 표준화 | 표준 단어·용어 제정 |
| 작의적 전용 금지 | 예정된 목적 외 사용 금지 |
| 단수형 사용 | 복수형 사용 지양 |

---

## 3절 엔터티 상세화

### 가. 식별자 확정

#### 식별자 확정 절차

```
키 엔터티 식별자 확정 → 메인 엔터티 식별자 확정 → 하위 엔터티 식별자 확정
```

#### 종속 관계 개념

| 구분 | 설명 |
|------|------|
| **절대 종속** | 내가 태어나기 위해 절대적으로 존재했어야 하는 부모 |
| **상대 종속** | 내가 태어날 때 존재하지 않아도 되는 부모 |
| **직접 종속** | 부모 엔터티와 1촌 관계 |
| **간접 종속** | 부모 엔터티와 1촌 초과 관계 |

> 행위 엔터티의 본질 식별자 = **절대 종속 + 직접 종속**

#### 인조 식별자 지정 기준
- 최대한 **범용적**인 값 사용
- **유일한 값**을 만들기 위해 사용
- 편의성·단순성 확보 목적
- 내부적으로만 사용하는 경우

---

### 나. 정규화

#### 정규화 단계 ★

| 정규형 | 조건 | 제거 대상 |
|--------|------|-----------|
| **1NF** | 모든 속성은 원자값, 반복 그룹 없음 | 다중값 속성, 반복 그룹 |
| **2NF** | 식별자가 아닌 속성은 식별자 전체에 완전 종속 | 부분 함수 종속 |
| **3NF** | 식별자를 제외한 속성 간 종속 없음 | 이행 함수 종속 |
| **BCNF** | 모든 결정자가 키 | Non-key 속성이 결정자인 경우 |

#### 정규화 이상 현상

| 이상 현상 | 설명 |
|-----------|------|
| **입력 이상** | 원치 않는 데이터도 함께 삽입해야 함 |
| **삭제 이상** | 일부 삭제 시 유지해야 할 정보까지 삭제 |
| **갱신 이상** | 일부 수정으로 정보 모순 발생 |

#### 정규화 장점
- 중복값·NULL값 최소화
- 새로운 요구 사항 발견 과정 지원
- 데이터 구조의 **안정성 최대화**

---

### 다. M:M 관계 해소

- M:M 관계는 최종 완성 모델에 **존재 불가**
- 새로운 **릴레이션 엔터티**를 추가하여 M:1 관계로 변경

#### 즉시 해소해야 하는 경우
1. 자식을 가져야 하는 경우
2. 추가적인 속성을 가져야 하는 경우

---

## 4절 이력관리 정의

### 가. 이력관리 대상 판단 기준

| 질문 | 해당 시 이력 관리 |
|------|-----------------|
| 변경 내역 감시 필요한가? | ✅ |
| 시간 경과에 따라 데이터가 변하는가? | ✅ |
| 시간 경과에 따라 관계가 변하는가? | ✅ |
| 과거 데이터를 조회할 필요가 있는가? | ✅ |
| 과거 버전을 보관해야 하는가? | ✅ |

### 나. 이력 데이터 종류

| 종류 | 설명 |
|------|------|
| **발생 이력** | 데이터가 발생할 때마다 저장 |
| **변경 이력** | 변경 전·후 차이를 확인하기 위해 저장 |
| **진행 이력** | 업무 진행에 따라 단계별 저장 |

### 다. 이력관리 형태 비교

| 구분 | 시점 이력 | 선분 이력 |
|------|-----------|-----------|
| **관리 내용** | 변경 발생 시각만 관리 | 시작~종료 시점까지 관리 |
| **데이터 성격** | 로그성 데이터 | 유효 기간 데이터 |
| **임의 시점 조회** | 비효율 | 유용 |
| **예시** | 특정 시점의 환율 | 특정 기간의 환율 |

### 라. 선분 이력 관리 유형 비교 ★

| 유형 | 설명 | 장점 | 단점 |
|------|------|------|------|
| **인스턴스 레벨** | 어떤 변경이든 전체 인스턴스를 새로 생성 | 한 번의 접근으로 스냅샷 참조 가능, 저장 용이 | 이벤트 모호, 저장 공간 낭비, 자식 정보 있으면 치명적 |
| **속성 레벨** | 이력 대상 속성에 변화가 생길 때만 이력 생성 | 변경 이벤트 분명, 통합 이력 관리 가능, 독립적 처리 | 여러 속성 조회 시 Merge 발생, 조건 검색 복잡 |
| **주제 레벨** | 유사하거나 연동 확률 높은 것별로 인스턴스 레벨 이력 관리 | 인스턴스·속성 레벨의 장점 수용, 독립적 처리, 확장성 확보 | 전체 참조 시 Merge 발생 |

### 마. 선분 이력 종료점 처리

| 처리 방법 | 장단점 |
|-----------|--------|
| **NULL 처리** | 논리적 타당, 단 비교 불가, 인덱스 미사용 → **성능 저하** |
| **최대치 부여** | 무한 계속 간주, 예) `9999-12-31`, **성능 유리** ← 권장 |

### 바. 이력관리 엔터티 구성 방식

| 방식 | 설명 |
|------|------|
| **통합 엔터티** | 현재 + 과거 데이터를 하나의 엔터티에 관리 |
| **분리 엔터티** | 현재와 과거를 별도 엔터티로 분리 |

---

## 5절 논리 데이터 모델 품질 검토

### 품질 기준 항목 ★ (최완일정준활)

| 기준 | 설명 |
|------|------|
| **정확성** | 표기법 준수, 업무 영역·요구사항 정확 반영 |
| **완전성** | 구성요소 정의 시 누락 최소화, 요구사항 반영 누락 없음 |
| **준거성** | 준수 요건 누락 없이 정확하게 준수 |
| **최신성** | 현행 시스템 최신 상태 반영, 이슈 지체 없이 반영 |
| **일관성** | 공통 데이터 요소가 전사 수준에서 한 번만 정의, 일관성 유지 |
| **활용성** | 이해관계자에게 충분한 의미 전달, 업무 변화 시 최소 설계 변경 |

### 논리 모델 검토 대상

| 검토 영역 | 검토 항목 |
|-----------|-----------|
| **엔터티** | 엔터티명, 엔터티정의, 통합수준, 권한, 발생건수/빈도, 관계 |
| **속성** | 속성명, 속성정의, 속성설명, 속성유형, 식별자정의, 법규준수 |
| **관계** | 관계명, 관계정의, 관계설명, 관계표현, 식별자상속, 외래키, 참조무결성 |
| **모델 전반** | 주제영역 적절성, 논리모델 상세화 |

---

# 제4장 물리 데이터 모델링

## 1절 물리 데이터 모델링 이해

### 가. 정의
- 논리 데이터 모델을 **각 DBMS의 특성·기능·성능**을 고려하여 DB의 물리적 구조로 변환
- 하나의 논리 모델 → **여러 개의 물리 모델** 생성 가능
- 하나의 논리 엔터티 → 경우에 따라 **여러 개의 테이블** 생성 가능

### 나. 논리-물리 모델 매핑

| 논리 모델 | 물리 모델 |
|-----------|-----------|
| 엔터티 | 테이블 |
| 속성 | 컬럼 |
| Primary UID | 기본키(PK) |
| Secondary UID | 유닉키(UK) |
| 관계 | 외래키(FK) |
| 비즈니스 규칙 | 제약조건 |

---

## 2절 물리 요소 조사 및 분석

### 파악 항목

| 항목 | 세부 내용 |
|------|-----------|
| **CPU** | 성능 및 집중 부하 발생 시간 |
| **Memory** | 전체 규모, 사용 가능 영역 |
| **Disk** | 전체 크기, 분할 형태, 활용률, 가용 공간 |
| **Network** | 처리 속도, 집중 부하 시간대, 동시 접속 수 |
| **OS 및 DBMS** | 버전 파악 |
| **DBMS 파라미터** | 파라미터 정보 파악 |
| **관리 요소** | 사용자 관리, 백업/복구, 보안 관리 정책 |

---

## 3절 논리-물리 모델 변환

### 가. 서브타입 변환 방법 비교 ★

| 방법 | 설명 | 유리한 경우 | 불리한 경우 |
|------|------|-------------|-------------|
| **슈퍼타입 기준** (통합) | 서브타입을 슈퍼타입에 통합하여 하나의 테이블 생성 | 액세스 간편, SQL 단순, 속도 향상, 임의 집합 처리 용이 | 컬럼 수 증가, 블록 수 증가, NOT NULL 제약 어려움, 인덱스 크기 증가 |
| **서브타입 기준** (분리) | 슈퍼타입 속성을 각 서브타입에 추가, 각각 테이블 생성 | 속성 선택성 명확, 서브타입 유형 구분 불필요, 전체 스캔 유리, 단위 테이블 크기 감소 | UNION 발생, 처리 속도 감소, 여러 테이블 처리, 복잡한 SQL, 뷰는 조회만 가능 |
| **개별타입 기준** (슈퍼+서브) | 슈퍼타입과 서브타입을 각각 별도 테이블로 생성, 1:1 관계 | 전체 데이터 처리 빈번, 서브타입 독립적 처리, 컬럼 수 너무 많은 경우 | 조인 증가 |

### 나. 관계 변환

#### 1:M 관계
- 1(One) 쪽의 PK를 M(Many) 쪽의 FK로 변환
- 필수 관계: `NOT NULL` 표시
- 선택 관계: NULL 허용

#### 1:1 관계
- Mandatory 반대쪽 테이블의 PK를 Mandatory 쪽에 FK로 추가
- 생성된 FK는 반드시 **Unique Key** 필수

#### 순환 관계
- 해당 테이블 내에 자기 자신을 참조하는 FK 컬럼 추가
- FK는 절대 **NOT NULL 불가**

#### 배타적(ARC) 관계 변환

| 방법 | 설명 | 특징 |
|------|------|------|
| **외래키 분리** | 각 관계를 별도 FK 컬럼으로 생성, 각각 Optional | 실제 FK 제약조건 생성 가능, 새 관계 추가 시 구조 변경 필요 |
| **외래키 결합** | 여러 관계를 하나의 FK 컬럼으로 생성 | 실제 FK 제약조건 생성 불가, User-Defined Trigger로 해결 |

### 다. 데이터 타입 선택

| 유형 | 세부 타입 선택 기준 |
|------|-------------------|
| **문자** | 영문만: VARCHAR / 유니코드: NVARCHAR / 4000자 이상: TEXT |
| **숫자** | 정수: INTEGER / 소수: DECIMAL / 화폐: MONEY / 참거짓: BOOLEAN |
| **날짜** | 일반: DATE/DATETIME / 정밀: TIMESTAMP |

### 라. 물리 DB 오브젝트 종류

| 오브젝트 | 설명 |
|----------|------|
| **DB** | 테이블의 집합, 주제영역·업무영역에 대응 |
| **스토리지 그룹** | 물리적 디스크를 묶은 그룹 (테이블스페이스 생성 시 지정) |
| **테이블스페이스** | 테이블이 생성되는 물리 영역 |
| **테이블** | 논리 엔터티에 대응하는 객체 |
| **컬럼** | 논리 속성에 대응하는 객체 |
| **인덱스** | 효율적 검색을 위한 색인 (기본키, 외래키 대상) |
| **뷰** | 테이블 재정의, 가상 테이블, 접근 권한 설정 가능 |

---

## 4절 반정규화

### 가. 개념
- 정규화된 엔터티·속성·관계에 대해 **성능 향상과 개발·운영 단순화**를 위해 중복·통합 등을 수행하는 기법
- 반정규화 대상: 테이블(병합/분할/추가), 컬럼(중복/계산/이력 추가), 관계(중복 관계)

---

### 나. 테이블 분할 (파티셔닝)

| 유형 | 기준 | 적용 상황 |
|------|------|-----------|
| **수평 분할** | 레코드(행) 기준 | 데이터 많고 특정 범위만 주로 액세스, 각 테이블을 다른 디스크에 위치 |
| **수직 분할** | 컬럼(열) 기준 | 컬럼 수가 많음, 조회·갱신 컬럼 구별, 자주 조회되는 컬럼 존재, 특정 컬럼 보안 필요 |

---

### 다. 중복 테이블 생성

| 유형 | 설명 | 적용 상황 |
|------|------|-----------|
| **집계(통계) 테이블** | GROUP BY·SUM 등 통계 정보를 미리 계산 저장 | 넓은 범위를 자주 처리, 수행 속도 저하 |
| **진행(이력) 테이블** | M:M 포함 처리 과정 추적·관리 | 조인이 빈번하고 처리 범위도 넓음 |
| **이력관리 테이블** | 이력 데이터 별도 보관 | 이력 조회가 빈번 |
| **인덱싱 테이블** | 인덱스 역할의 테이블 | 접근 경로 단축 목적 |

#### 중복 테이블 생성 판단 기준
- 정규화로 인해 수행 속도가 느려지는 경우
- 많은 범위를 **자주 처리**해야 하는 경우
- 요약 자료만 주로 요구되는 경우
- 인덱스 조정·클러스터링으로 해결 불가한 경우

---

### 라. 중복 컬럼 생성

#### 적용 상황
- 빈번하게 조인을 일으키는 컬럼
- 조인 범위가 다량인데 온라인 처리가 필요한 경우
- 액세스 조건으로 자주 사용되는 컬럼
- 접근 경로 단축을 위해 **부모 테이블 컬럼을 자식 테이블에 중복**
- 계산된 값의 이용이 빈번한 경우

> 복사된 컬럼의 도메인은 원본 컬럼과 **동일** 유지

---

## 5절 물리 데이터 모델 품질 검토

### 품질 기준 항목 (논리 모델과 동일) ★

| 기준 | 설명 |
|------|------|
| **정확성** | 표기법 준수, 업무 영역·요구사항 정확 반영 |
| **완전성** | 구성요소 정의 시 누락 최소화 |
| **준거성** | 준수 요건 누락 없이 정확하게 준수 |
| **최신성** | 현행 시스템 최신 상태 반영 |
| **일관성** | 공통 데이터 요소 전사 수준에서 한 번만 정의, 일관성 유지 |
| **활용성** | 충분한 의미 전달, 업무 변화 시 최소 설계 변경 |

### 물리 모델 검토 대상

| 검토 영역 | 검토 항목 |
|-----------|-----------|
| **테이블** | 테이블명, 테이블설명, 통합수준, 권한, 발생건수/빈도, 법규준수, 요구사항 추적 |
| **컬럼** | 컬럼명, 컬럼정의, 기본키, 유닉키, 법규준수, 도메인정의, 추출컬럼정의, 요구사항 추적, 오너쉽 |
| **외래키** | 참조무결성 규칙 정의, 요구사항 추적, 외래키 일관성 |
| **모델 전반** | 반정규화, 인덱스 정의, 스토리지 정의 |

---

## 📚 핵심 암기 요약

### 데이터 모델링 3단계 비교

| 구분 | 개념 모델링 | 논리 모델링 | 물리 모델링 |
|------|------------|------------|------------|
| **목적** | 핵심 구조 파악 | 비즈니스 규칙 명확화 | DB 구현 최적화 |
| **구성 요소** | 핵심 엔터티, 관계 | 엔터티, 속성, 관계, 식별자 | 테이블, 컬럼, 인덱스 |
| **주요 작업** | 주제영역, 엔터티 도출 | 정규화, M:M 해소, 이력관리 | 반정규화, 인덱스, 데이터 타입 |
| **기술 독립성** | 완전 독립 | 독립 | DBMS 의존 |

### 정규화 단계 요약

| 정규형 | 제거 대상 | 핵심 키워드 |
|--------|-----------|------------|
| 1NF | 반복 그룹, 다중값 | **원자값** |
| 2NF | 부분 함수 종속 | **완전 종속** |
| 3NF | 이행 함수 종속 | **속성 간 종속 제거** |
| BCNF | Non-key 결정자 | **모든 결정자가 키** |

### 이력관리 선분 이력 3가지 유형

| 유형 | 관리 단위 | 특징 |
|------|-----------|------|
| 인스턴스 레벨 | 전체 인스턴스 | 단순, 공간 낭비 |
| 속성 레벨 | 개별 속성 | 이벤트 명확, Merge 발생 |
| 주제 레벨 | 주제 단위 그룹 | 두 유형의 장점 결합 |

### 반정규화 분류

| 대상 | 기법 |
|------|------|
| **테이블** | 병합, 분할(수평/수직), 추가(집계/진행/이력/인덱싱) |
| **컬럼** | 중복 컬럼, 계산 컬럼, 이력 컬럼 추가 |
| **관계** | 중복 관계 추가 |

---

> 📝 **학습 TIP**  
> - 좋은 데이터 모델 요소: **완중업재 안의간통** (완전성·중복배제·비즈니스룰·재사용·안정성·의사소통·간결성·통합성)  
> - 품질 기준: **최완일정준활** (최신성·완전성·일관성·정확성·준거성·활용성)  
> - M:M 관계는 최종 모델에 **존재 불가** → 반드시 해소
> - 선분 이력 종료점: NULL보다 **최대치(9999-12-31) 권장**
